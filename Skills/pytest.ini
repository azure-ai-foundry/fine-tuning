[pytest]
markers =
    live: hits the real Foundry Data Generation API (requires FOUNDRY_PROJECT_ENDPOINT)
    slow: takes >30 seconds to run

# Default: run only non-live (fast) tests. Override with -m live or -m '' for everything.
addopts = -m "not live"
